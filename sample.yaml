---
- hosts: cloudera
  gather_facts: no
  tasks:
    # - debug: msg="{{ hostvars[inventory_hostname]['ansible_nodename'] }}"
    - name: tests
      include_role:
        name: cloudera-install-agent
    # - name: check file 
    #   find: 
    #     path: /var/www/html
    #     file_type: directory
    #     recurse: yes
    #     patterns: "repodata"
    #   register: matches
    # - name: debug 
    #   debug:
    #     msg: "{{ matches.files[0].path | dirname c| regex_replace('/var/www/html/','') }}"